// Salin file ini menjadi config.js dan isi dengan konfigurasi backend kamu.
// JANGAN masukkan API key rahasia ke JavaScript yang dikirim ke browser.
// File config.js sebaiknya tidak di-commit ke repository public.
window.APP_CONFIG = {
  apiBaseUrl: "",
  // Jika menggunakan backend/proxy, endpoint dapat ditempatkan di sini.
  cloudEndpoint: ""
};
